{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<h1 class="d-flex justify-content-center border-radius margin-auto">Bienvenue sur Bestio !</h1>
<div class="separator"></div>
<div class="d-flex flex-row-nowrap align-items-center justify-content-around">
    {% for categorie in categories %}
        <a href="{{ path('app_categorie_show', {'categorie': categorie.nomCategorisation}) }}">
            <div class="categorie-flip">
                <div class="categorie-flip-inner">
                    <div class="categorie-flip-front">
                        {# Choix de l'image selon la catégorie #}
                        {% if categorie.nomCategorisation == 'Reptiles' %}
                            <img src="{{ asset('images/reptile.png') }}" alt="Image de serpent" class="categorie-img">
                        {% elseif categorie.nomCategorisation == 'Oiseaux' %}
                            <img src="{{ asset('images/oiseau.png') }}" alt="Image d'oiseau" class="categorie-img">
                        {% elseif categorie.nomCategorisation == 'Poissons' %}
                            <img src="{{ asset('images/poisson.png') }}" alt="Image de poisson" class="categorie-img">
                        {% elseif categorie.nomCategorisation == 'Mammifères' %}
                            <img src="{{ asset('images/mammifere.png') }}" alt="Image de lion" class="categorie-img">
                        {% elseif categorie.nomCategorisation == 'Amphibiens' %}
                            <img src="{{ asset('images/grenouille.png') }}" alt="Image de grenouille" class="categorie-img">
                        {% endif %}
                    </div>
                    <div class="categorie-flip-back d-flex align-items-center justify-content-center">
                        <span class="categorie-nom">{{ categorie.nomCategorisation }}</span>
                    </div>
                </div>
            </div>
        </a>
    {% endfor %}
    {# Bulle "Tout" à la fin #}
    <a href="{{ path('app_classe') }}">
        <div class="categorie-flip">
            <div class="categorie-flip-inner">
                <div class="categorie-flip-front">
                    <img src="{{ asset('images/patte.png') }}" alt="Image patte" class="categorie-img">
                </div>
                <div class="categorie-flip-back d-flex align-items-center justify-content-center">
                    <span class="categorie-nom">Tout</span>
                </div>
            </div>
        </div>
    </a>
</div>
<div class="separator"></div>
<div class="description-site d-flex justify-content-center mb-20">
    <p style="max-width: 700px;">
        Bestio est un site dédié à la découverte du monde animal. Explorez les différentes catégories, classes et espèces pour en apprendre plus sur la biodiversité et les particularités de chaque animal. Notre objectif : rendre la connaissance animale accessible à tous !
    </p>
</div>
<div class="separator"></div>
{% if animalDuJour %}
<div class="animal-jour d-flex flex-column align-items-center mb-30">
    <h2 class="border-radius">Animal du jour</h2>
    <div class="animal-card">
        <img src="{{ animalDuJour.image }}" alt="{{ animalDuJour.nomEspece }}">
        <h3 class="p-20">{{ animalDuJour.nomEspece }}</h3>
        <p class="pb-20">{{ animalDuJour.description|slice(0, 120) ~ '...' }}</p>
        <a href="{{ path('app_show_espece', {
            'categorie': animalDuJour.classe.appartenir.nomCategorisation,
            'nom': animalDuJour.classe.nom,
            'nom_espece': animalDuJour.nomEspece
        }) }}" class="btn btn-primary-stroke p-20">Découvrir</a>
    </div>
</div>
{% endif %}
{% endblock %}