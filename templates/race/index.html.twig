{% extends 'base.html.twig' %}

{% block title %}Ajouter une race{% endblock %}


{% block body %}
<div class="example-wrapper">
   {{ form_start(formRace) }}
       {{ form_widget(formRace) }}
       <button type="submit" class="btn btn-primary">Enregistrer</button>
   {{ form_end(formRace) }}
</div>
{% endblock %}

