{% extends "base.html.twig" %}

{% block title %}Profil de {{ user.pseudo }} {% endblock %}

{% block body %}
    <div class="block mb-20">
        <h2 class="pb-30">Profil de {{ user.pseudo }}</h2>
        <div class="d-flex flex-row-nowrap">
            <img src="{{ user.photo ?? '/images/profil.webp' }}" alt="Photo de profil" class="mr-10 profile-picture">
            <ul>
                <li class="mb-10"><strong class="mr-10">Pseudo :</strong>{{ user.pseudo }}</li>
                <li class="mb-10"><strong class="mr-10">Email :</strong>{{ user.email }}</li>
            </ul>
        </div>
    </div>

    {# <div class="block mb-20">
        <h3 class="pb-30">Questions</h3>
        <ul>
        {% for question in user.questions %}    
        <li>{{ question.title }}</li>            
        {% endfor %}
        </ul>
    </div>

    <div class="block mb-20">
        <h3 class="pb-30">Commentaires</h3>
        <ul>
        {% for comment in user.comments %}
        <li>{{ comment.content }}</li>            
        {% endfor %}
        </ul>
    </div> #}
{% endblock %}


